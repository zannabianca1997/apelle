meta {
  name: Default alternate user exists
  type: http
  seq: 4
}

get {
  url: {{endpoint}}/me
  body: none
  auth: basic
}

auth:basic {
  username: user2
  password: password
}

tests {
  test("should exist", function () {
    expect(res.getStatus()).to.be.equal(200);
  });
  
  
  test("should return json", function () {
    expect(res.getBody())
      .to.be.an('object')
      .to.deep.own.include({
        "roles": []
      });
  });
}

docs {
  Check the default user exists
}
